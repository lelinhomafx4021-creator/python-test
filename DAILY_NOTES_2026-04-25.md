# Daily Notes 2026-04-25

## 2026-04-26 处理结果

- 已修正 Python SSE 响应头，明确启用 `text/event-stream; charset=utf-8`，并关闭代理缓冲。
- 已修正 Java 网关 SSE 转发方式，改为按 `ServerSentEvent<String>` 解析上游事件，而不是把 Python 原始 SSE 文本当普通字符串再包一层。
- 已恢复工作流步骤中的 emoji 状态文案，便于直接验证字符链路。
- 已为前端补充 emoji 字体兜底，并增加 `done` 事件关闭与思考步骤去重逻辑。

### 当前结论

- 流式输出问题的主要断点基本定位在 Java 网关对 Python SSE 的二次错误封装。
- emoji 展示问题已同时从后端编码、网关透传、前端字体三个层面做了修复。
- Python 导入和语法检查通过，Java 编译通过。
- 前端构建命令仍被现有 TypeScript 配置版本不兼容阻塞，不是这次 SSE/emoji 修复本身引入的新错误。

## 今天完成的事

- 将项目中的核心提示词从内联字符串升级为 LangChain `ChatPromptTemplate` 写法。
- 为关键节点增加了受约束输出，避免完全依赖自然语言字符串解析。
- 新增统一的提示词资产文件：`aipy2/app/prompts/investor_prompts.py`。
- 梳理了 `AgentState`、节点 `return {...}`、LangGraph 状态合并之间的关系。

## 今天遇到的问题

### 1. 流式输出展示不完整或前端感知不明显

- 当前后端虽然通过 `astream(..., stream_mode="updates")` 在推送节点更新，但前端侧是否按阶段正确消费、刷新和渲染，还需要继续排查。
- 需要确认问题到底出在：
  - 后端没有持续稳定地产出增量事件
  - 网关层或 SSE 转发层吞掉了中间事件
  - 前端拿到了事件但没有正确渲染

### 2. AI 返回的 emoji / 图标无法正常展示

- 之前节点里的 `step` 文案包含 emoji，例如导航、回答、评审等图标。
- 当前展示链路中，这些字符存在不显示、乱码或被吞掉的现象。
- 需要重点排查：
  - Python 文件编码
  - Java 网关转发编码
  - 前端渲染字体或字符集兼容性
  - SSE / JSON 序列化过程中是否发生编码损坏

### 2.1 emoji 展示测试样例

- 基础样例：😀 😄 🤖
- 工作流样例：🧭 🔍 🧠 ✍️ ✅ ❌
- 投研样例：📈 📉 💰 📰
- 混合文本样例：正在路由中 🧭，正在检索中 🔍，正在生成中 ✍️

### 3. 受约束输出已经接入，但还没有做真实业务问答回归

- 代码层面的导入和语法检查已通过。
- 还没有对真实问题做端到端验证。
- 需要观察路由、改写、评审这些节点在真实模型返回下是否足够稳定。

## 明天要解决的问题

### 优先级 P0

- 跑一遍真实对话链路，确认流式输出是否能从 Python 后端一路传到前端。
- 定位 emoji / 图标无法展示的具体断点，确认是编码问题、渲染问题还是转发问题。

### 优先级 P1

- 对 `intent`、`rewrite`、`critic` 三个受约束节点做真实返回测试。
- 检查解析失败时的 fallback 是否足够稳，不影响主流程。

### 优先级 P2

- 视情况决定是否保留 emoji 文案。
- 如果兼容性差，可以改为纯文本状态前缀，例如：
  - `路由中`
  - `检索中`
  - `生成中`
  - `评审中`

## 明天建议排查顺序

1. 先用浏览器或接口工具直接看后端 SSE 原始输出。
2. 再看 Java 网关是否完整转发中间事件。
3. 最后看前端事件监听和渲染逻辑。
4. 对 emoji 问题分别验证后端日志、网关响应、前端页面三个位置是否一致。

## 当前判断

- LangGraph 状态流转本身已经基本清楚。
- 现在的主要问题不是提示词框架本身，而是“流式链路”和“字符展示链路”的前后端联调。
