# WSL 与 Windows 文件互动指南

作为一名大学生程序员，掌握 WSL (Windows Subsystem for Linux) 的文件互操作性是提升开发效率的关键。以下是针对你问题的详细解答：

## 1. 路径怎么互通？

WSL 和 Windows 其实是两个并行的世界，但微软通过“挂载”让它们可以互相串门。

### 从 WSL 访问 Windows 文件（最常用）
所有的 Windows 磁盘都被自动挂载在 `/mnt/` 目录下。
- **C 盘**: `/mnt/c/`
- **D 盘**: `/mnt/d/`
- **示例**: 如果你的项目在 `D:\ai-investor`，在 WSL 里的路径就是 `/mnt/d/ai-investor`。

### 从 Windows 访问 WSL 文件
在文件资源管理器的地址栏输入：
- `\\wsl$\`
你会看到你安装的 Linux 发行版（如 Ubuntu）的文件夹，就像访问网络共享文件夹一样。

---

## 2. 如何运行 Windows 下的 Dockerfile？

既然现在你知道了路径映射，运行 Dockerfile 就很简单了：

1. **打开 WSL 终端**。
2. **跳转到项目目录**:
   ```bash
   cd /mnt/d/ai-investor
   ```
3. **运行 Docker 命令**:
   ```bash
   docker build -t 我的镜像名 .
   ```
   *注意：末尾的 `.` 代表当前目录，也就是 `/mnt/d/ai-investor`，Docker 会自动在那里找 `Dockerfile`。*

### 💡 重要提示：Docker Desktop 设置
确保你的 **Docker Desktop** 设置中：
- `General` -> `Use the WSL 2 based engine` 已勾选。
- `Resources` -> `WSL Integration` 中勾选了你正在使用的 Linux 发行版。

---

## 3. 面试加分知识点 🎓

在面试中，如果面试官问到 WSL，你可以提到以下几点展现你的深度：

- **I/O 性能损耗**: WSL 2 访问 Windows 文件系统（跨 9P 协议）会有明显的性能下降。**面试话术**：“在进行大规模代码编译（如 Java 项目构建）时，建议将项目放在 `/home/username/` 这种 WSL 原生文件系统中，比在 `/mnt/d/` 下运行快 10 倍以上。”
- **WSL 1 vs WSL 2**: WSL 1 是翻译层（把 Linux 系统调用翻译成 Windows 调用），而 WSL 2 是轻量级虚拟机（有着真实的 Linux 内核）。后者对 Docker 的支持要好得多。
- **环境一致性**: 为什么要折腾 WSL？因为大多数线上服务器是 Linux。在 WSL 里跑 Docker 可以确保“开发环境”与“生产环境”高度一致，避免“在我电脑上好好的，上线就挂了”的问题。

---

## 下一步建议
你可以先试着执行 `ls /mnt/d/ai-investor` 看看能不能列出文件。如果有任何权限问题或 Docker 运行报错，随时问我！
